(function() {
    var foo = 'bar';
     var index = 0;
    while (index < foo.length) {
        /**
         * Not incorrect
         */
          console.log(foo.charAt(index));
        index++;
    }
});
